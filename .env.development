# Same-origin so requests go through the dev server's /api proxy, which picks a
# Render IP that is actually reachable (see vite.config.ts). Override in
# .env.development.local to point at a backend running locally.
VITE_API_URL=/api
