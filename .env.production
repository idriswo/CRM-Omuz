# Same-origin: Vercel rewrites /api to the Render backend (see vercel.json).
# Going through that proxy stops the browser from picking one of Render's two
# Cloudflare IPs that is unreachable from some networks and dying with
# ERR_CONNECTION_CLOSED — the same problem the dev server's proxy solves.
VITE_API_URL=/api
